# pages

One slice per screen. A page composes widgets, features and entities; it holds no reusable logic of
its own and is the layer where concrete transports get injected into the components below.

Current slice: `dashboard` — the worked example of these conventions.
