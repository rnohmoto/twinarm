# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test output
coverage
test-results
playwright-report
blob-report

# The root .gitignore carries Python build patterns (build/, dist/, lib/) that
# would otherwise swallow source directories here — src/shared/lib/ among them.
!/src/**
