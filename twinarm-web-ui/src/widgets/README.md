# widgets

Self-contained blocks that compose several features or entities into one piece of a page — a
telemetry panel with its own controls, for example.

Empty on purpose. Compose in `pages` until a block is reused by two pages, then extract it here.
